class Data
{
public:
	int mouth;
	int day;
	const int year = *; <-- тут заместо * должно было быть значение но можно инициализировать константу через конструктор(точнее когда его вызываешь).

public:
Data(int num) : year{10} <-- вот тут мы константу year инициализировали числом 10.
}